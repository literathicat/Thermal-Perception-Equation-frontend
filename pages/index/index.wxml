<!--pages/index/index.wxml-->
<view class="container">
  <!-- 背景图片 -->
  <image src="/image/首页-背景.png" class="background" mode="aspectFill"></image>
  <!-- 顶部用户和天气信息 -->
  <view class="header">
    <image src="/image/图层3.png" mode="widthFix" class="flexible-image" style="width:100%"></image>
    <!-- 四个图标放在天气信息下方 -->
    <view class="icon-grid">
      <block wx:for="{{iconsTop}}" wx:key="name">
        <view class="icon-item" bindtap="navigateTo" data-url="{{item.url}}">
          <image src="{{item.icon}}" mode="aspectFit"></image>
          <text>{{item.name}}</text>
        </view>
      </block>
    </view>
  </view>
  <!-- 今日穿搭签 -->
  <view class="outfit-sign">
    <image src="/image/首页-中部标签.png" mode="widthFix" class="flexible-image" style="width:100%"></image>
 
  <view class="outfit-content">
    <image src="{{outfitRecommend.image}}" mode="aspectFill" class="outfit-image"></image>
      <view class="outfit-sign-container">
        <image src="/image/首页-今日穿搭签.png" class="outfit-sign-image" mode="aspectFit"></image>
      </view>
    </view>
  </view>

  <!-- 底部导航栏图片 -->
  <image src="/image/首页-底部悬浮导航栏.png" class="bottom-nav-image" mode="aspectFit"></image>
</view>